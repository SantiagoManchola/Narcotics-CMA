const header=document.getElementById("header");function isScrolled(e){window.scrollY>e?header.classList.add("scrolled"):header.classList.remove("scrolled")}window.addEventListener("scroll",()=>{isScrolled(0)});